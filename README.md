# c2pa-sample

```sh
git clone https://github.com/contentauth/c2pa-rs /tmp/c2pa-rs
mv /tmp/c2pa-rs/sdk/tests .
cargo run --bin read_minifest_store
cargo run --bin add_manifest_to_file 
```

```sh
openssl x509 -text -in <certificate path>
```
